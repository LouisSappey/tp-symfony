<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin</title>
    <!-- Add Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-4">
    <div class="container mx-auto">
        <div class="bg-white p-4 rounded shadow-md">
            <h1 class="text-xl mb-4">Espace Administrateur</h1>
            <p>Bienvenue, {{ app.user.username }} !</p>
            <a href="{{ path('page_homepage') }}" class="mt-2 block bg-blue-500 text-white px-4 py-2 rounded">Retour à l'accueil</a>
            <a href="{{ path('app_playlist_index') }}" class="mt-2 block bg-green-500 text-white px-4 py-2 rounded">Modifier les Playlists</a>
            <a href="{{ path('app_artiste_index') }}" class="mt-2 block bg-blue-500 text-white px-4 py-2 rounded">Modifier les Artistes</a>
            <a href="{{ path('app_album_index') }}" class="mt-2 block bg-yellow-500 text-white px-4 py-2 rounded">Modifier les Albums</a>
            <a href="{{ path('app_commentaire_index') }}" class="mt-2 block bg-red-500 text-white px-4 py-2 rounded">Modifer les commentaires</a>
        </div>
    </div>
</body>
</html>
